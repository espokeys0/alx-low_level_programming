#include "main.h"

int find_sqrt(int num, int root);
int _sqrt_recursion(int n);

/**
 *find_sqrt - find the naturel square
 *@num: the number
 *@root: the root 
 *Return: naturel square
 */

find_sqrt(int num, int root)
{
if ((root * root) == num)

return (root);
}
