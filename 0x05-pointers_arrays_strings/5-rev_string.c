#include <stdio.h>
#include "main.h"

/**
*puts2 - print every other character
*@str: String
Ã*Return: nothing
*/

void puts2(char *str);
{
int i = 0;

while (*(str + i) != '\	0')
{
if (i % 2 == 0)
}
putchar(*(str + i));
putcher(10);
}
